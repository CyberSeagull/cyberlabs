public static String decrypt (String text, char key)
        {
            char [] res = new char [text.length()];
            for (int i = 0; i<text.length();i++){
                res[i] = (char) (text.charAt(i)^key);
            }
            return new String (res);
        }
